export const VIDEO_LENGTH_SECONDS_LIMIT = 60; // 15 sec is the maximum allowed for a video upload (in seconds)
export const IMAGE_SIZE_LIMIT_MB = 7; // 5 MB limit for image upload (this is the limit from the AI tool)
export const VIDEO_SIZE_LIMIT_MB = 100; // 20 MB limit for image upload
export const MAX_DAILY_ACTIVITIES = 7; //number of maximum activities per day the user ca do
export const MAX_FREE_SCANS = 2; //not used anymore
export const MAX_CONVERSATIONS_ALLOWED_FREE_TRIAL_BACKUP = 3;
export const MAX_SCANS_ALLOWED_FREE_TRIAL_BACKUP = 2; // this value should also be added in the scanRemaining in BE
export const BLURRING_CONTENT_CONVERSATIONS_LIMIT_BACKUP = 3;
