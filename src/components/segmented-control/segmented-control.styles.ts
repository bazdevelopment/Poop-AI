import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    height: 80,
  },
  activeBox: {
    position: 'absolute',
    borderRadius: 20,
    elevation: 3,
    height: '100%',
  },
});
